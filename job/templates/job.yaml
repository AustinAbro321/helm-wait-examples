apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.jobName }}
spec:
  template:
    metadata:
      name: sleep-job
    spec:
      containers:
      - name: sleep
        image: busybox
        command: ["sh", "-c", "sleep 5"]
      restartPolicy: Never